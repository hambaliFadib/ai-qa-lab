# API Payload Patterns

- Transaction Mapping create payload should only be trusted after a live POST observation from the migrated runtime.
- Dropdown-backed modules often send identifiers rather than labels.
- Capture payload patterns from runtime evidence and store only the reusable structure, not sensitive values.
