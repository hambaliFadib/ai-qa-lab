# Migration 2026-04-05

This archive bundle stores content moved out of the active workflow during the Learning Ledger v1 migration.

## Contents

- `legacy-runtime-scripts/` keeps old playground scripts, empty helper files, and exploratory variants that were not selected as the main runtime path.
- `legacy-testing/` keeps the pre-migration testing folder shell after its active subfolders were moved into `06-testing/`.
- `legacy-root-artifacts/` keeps old root-level artifacts that were imported cautiously instead of merged destructively.
- `assistant-misplaced-files/` keeps temporary files created during migration debugging outside the repo root.

Nothing in this archive was deleted; items were moved here to keep the active structure clean while preserving recovery options.
