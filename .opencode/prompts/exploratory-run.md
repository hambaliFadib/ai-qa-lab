Start exploratory work by reading runtime handoff, active module, blockers, memory, and module knowledge first.

During the run:

- classify access before judging the feature
- focus on one module
- capture UI, API, and validation evidence
- keep notes under `01-runtime/artifacts/adhoc-notes/`

After the run:

- update runtime state
- update memory
- refresh module knowledge
- append a ledger block
