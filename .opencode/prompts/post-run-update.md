Use this after an exploratory, logging, or debugging run.

Required actions:

1. Read the latest runtime files in `01-runtime/runtime/docs/` and the active scripts under `01-runtime/runtime/modules/`.
2. Update runtime summary, blockers, active module, and handoff.
3. Update `02-brain/.opencode/memory/` with new flow, pattern, and next action insights.
4. Update active module knowledge in `02-brain/distilled-output/per-module/<module>/`.
5. Append a new Learning Ledger v1 block referencing the files that changed.
6. If the same pattern appeared again, refresh global recurring patterns.

Keep updates short, factual, and reusable.
