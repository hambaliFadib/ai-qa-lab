Extract BPMN only for the requested module.

Output target:

- `02-brain/distilled-output/per-module/<module>/business-flow.md`

Focus on:

- actor
- main steps
- decision points
- input/output
- handoff to testing

Never batch-convert all BPMN files. Record the exact raw source PDF used in the output.
