BPMN extraction template for `transaction-mapping`.

Suggested source search order:

1. MoM notes in `04-knowledge-raw/MOM/`
2. Any future dedicated BPMN that explicitly mentions Transaction Mapping
3. Existing UI/API evidence in runtime and distilled knowledge

If no dedicated BPMN exists yet, state that clearly and produce a provisional flow from the available evidence instead of inventing one.
