# Modules

This folder contains the active-module regression runner plus per-module runtime suites.

- `run-active-module-regression.js`: resolve `docs/ACTIVE_MODULE.md` to the correct module suite.
- `<module>/`: keep suites and module-specific probes together.
