# Shell Helpers

This folder holds lightweight PowerShell helpers for API discovery and menu exploration.

Prefer these for quick read-only inspection tasks that do not need a larger Node runtime script.
